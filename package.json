{
  "scripts": {
    "compile": "webpack",
    "deploy": "rm -rf functions && yarn compile && apex deploy"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.6",
    "aws-lambda": "^0.1.2",
    "random-name": "^0.1.2",
    "typescript": "^2.9.2"
  },
  "devDependencies": {
    "@types/node": "^10.3.3",
    "ts-loader": "^4.4.1",
    "webpack": "^4.12.0",
    "webpack-cli": "^3.0.6",
    "webpack-command": "^0.2.1"
  }
}
